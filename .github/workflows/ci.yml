***REMOVED*** ============================================================================
***REMOVED***  Schnitzery Portal — Continuous Checks
***REMOVED***  Runs automatically on every push and pull request:
***REMOVED***  unit tests → type-check → production build. A failure is caught HERE,
***REMOVED***  in GitHub, before it ever reaches Vercel or your users.
***REMOVED*** ============================================================================
name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Test, type-check & build
    runs-on: ubuntu-latest

    steps:
      - name: Check out the code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci --ignore-scripts

      - name: Unit tests (vitest)
        run: npm test

      - name: Lint (eslint)
        run: npm run lint

      - name: Type-check (tsc)
        run: npx tsc --noEmit

      - name: Dependency audit
        run: npm audit --audit-level=high
        continue-on-error: true   ***REMOVED*** report, don't block — flip to false once clean

      - name: Production build (next build)
        run: npm run build
        env:
          NEXT_PUBLIC_SUPABASE_URL: https://placeholder.supabase.co
          NEXT_PUBLIC_SUPABASE_ANON_KEY: placeholder-anon-key
          SUPABASE_SERVICE_ROLE_KEY: placeholder-service-key